{
  "name": "scp-db-viewer",
  "version": "1.0.0",
  "description": "SCP Foundation Database Viewer — Terminal Edition",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc && copyfiles -f src/pages/*.html dist/pages/ && copyfiles -f src/components/styles/*.css dist/components/styles/ && copyfiles -f src/components/images/*.png dist/components/images/",
    "start": "npm run build && electron .",
    "dev": "tsc --watch & copyfiles -f src/pages/*.html dist/pages/ && electron ."
  },
  "dependencies": {
    "axios": "^1.6.7",
    "cheerio": "^1.0.0-rc.10",
    "copyfiles": "^2.4.1"
  },
  "devDependencies": {
    "electron": "^29.0.1",
    "typescript": "^5.3.3"
  }
}
